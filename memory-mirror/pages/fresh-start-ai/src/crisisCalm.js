export function crisisCalm(text) {
  return \(gentle, slow, grounding tone) \\;
}

export function orientationCue() {
  const now = new Date();
  const hours = now.getHours();
  const timeOfDay =
    hours < 12 ? 'morning' :
    hours < 18 ? 'afternoon' :
    'evening';

  return \You're safe. It's the \. You're not alone.\;
}
